<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">

        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>

       {# container fluid permet de prendre toute la largeur de la page #}
        <div class="container-fluid">
            <header>

                {# Création d'une variable sur Twig #}
                {% set menu = ["Accueil","Mon Profil","Villes","Sites"] %}

                <nav class="navbar navbar-expand-lg bg-light">
                    <div class="container-fluid">
                    <a class="navbar-brand" href="#">Groupe JDS</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav">
                        {# Début de la boucle #}
                            {% for element in menu %}   
                            <li class="nav-item">
                                <a class="nav-link active" aria-current="page" href="#">{{element}}</a>
                            </li>
                            {% endfor %}
                        {# Fin de la boucle #}
                        </ul>
                    </div>
                    </div>
                </nav>

            </header>

            <main>
                <div class="row title text-center mt-5">
                    <h1>Accueil</h1>
                </div>
            </main>

            <footer>
                <div class="row text-center">
                    <p>
                        {# le &copy permet de faire le copyright #}
                        {# le | permet d'utiliset le filtre dates #}
                        Groupe JDS &copy; {{"now"|date("Y")}}
                    </p>
                </div>
            </footer>

        </div>


        {% block body %}{% endblock %}
        
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js" integrity="sha384-pprn3073KE6tl6bjs2QrFaJGz5/SUsLqktiwsUTF55Jfv3qYSDhgCecCxMW52nD2" crossorigin="anonymous"></script>

    </body>
</html>
